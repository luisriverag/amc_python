# Compatibility matrix

Statuses: **unsupported**, **investigating**, **prototype**, **partial**, **verified**,
or **intentionally omitted**. A prototype has local behavior but no upstream
evidence. “Verified” for an upstream format requires an upstream-generated fixture
and a documented cross-application test; synthetic tests alone qualify only as
partial. “Verified (internal only)” applies solely to Python-owned contracts.

| Capability | Status | Evidence | Next action |
|---|---|---|---|
| Internal JSON v1 read/write | verified (internal only) | Format specification, semantic round trip, strict envelope/row validation, duplicate-key rejection, finite JSON enforcement, and tolerance of a leading UTF-8 BOM on read | Add an explicit version-migration framework before a v2 format is needed |
| Atomic output replacement | partial | JSON, CSV, and XML serialization failures plus an injected replacement failure; every atomic writer in the package (JSON/CSV/XML/HTML saves, catalog copy, picture export, TSV loan-history export, GUI preferences, and script settings) now fsyncs its destination directory entry after replacement, not just the file contents, matching the native `.amc` writer; a denied parent-directory creation or temp-file creation now has injected-failure tests for every `storage.py` writer, `copy_catalog`, and the native writer, confirming an unwrapped `PermissionError`/`OSError` and an untouched destination | Test concurrency |
| Native `.amc` read | investigating | User-designated source baseline drives fixed-record 1.0–3.0 and modern 3.1–4.2 parsing, typed 4.2 writer/composer/certification/file-path/user-rating/color-tag fields, pre-3.0 linked-picture and borrower sidecars, bounded collections with CLI-configurable import budgets, exhaustive synthetic record-truncation checks for 1.0–3.0 and 4.2, deterministic byte-mutation checks, native→JSON retention, preservation of undefined CP-1252 bytes, explicit caller-selected codecs during import, and symmetric structural-failure diagnostics (both the fixed-record and modern readers now wrap an invalid parsed movie value as `CorruptCatalogError` rather than an unwrapped exception escaping `validate_catalog`); loaded and round-tripped cleanly against a genuine AMC 4.2 native export locally (not committed to the repository, see `docs/PORT_AUDIT.md` finding 34), and against a genuine, committed empty/one-movie 3.5/4.1/4.2 fixture set (`tests/fixtures/native-empty-one-movie/`, this port's first `upstream-generated`-origin native fixtures), which found and fixed a real `-1`-sentinel-vs-`None` bug in `year`/`length`/`video_bitrate`/`audio_bitrate`/`media_count` confirmed against the checked-in Delphi source (finding 38); a hand-authored `synthetic`-origin fixture pair informed by finding 34 is registered at `tests/fixtures/edge-cases/` (finding 35), and finding 38's fix has both synthetic byte-level and genuine-fixture regression tests; separately, AMC's own official demo catalog, now committed at `tests/fixtures/native-sample-catalog/` (`Sample_4.2.0.amc`/`Sample_3.5.1.amc`, this port's first genuine, redistribution-cleared populated-catalog native fixtures), exposed a total-parse-failure bug for any catalog with a `List`-type custom field — `_read_custom_field` compared `field_type` against bare `"list"` instead of upstream's actual `"ftList"` (confirmed in the checked-in Delphi source), corrupting every subsequent byte offset until the reader crashed on a garbage string length; fixed on both the read and write side, and five *existing* synthetic tests that had silently synthesized the same wrong `"List"` string (passing cleanly against the buggy code) corrected to match reality (finding 39); genuine-fixture regression tests now cover the populated-movie read, all eight represented custom-field types and values, embedded-picture decoding for every movie, and a full read/write/reread round trip for both files — this is not upgraded to `verified` because there is still no documented cross-application test (Python-written output reopened in genuine AMC) | Determine real catalog code pages across more than one real catalog; cross-check every remaining version; add property-framework fixture mutation coverage; add a documented cross-application (write, then reopen in real AMC) test |
| Non-destructive inspection/validation | partial | JSON/XML/CSV inspection plus source-derived native 1.0–4.2 header probe; CLI-configurable `--max-input-bytes` rejects an oversized file before JSON/native parsing starts | Validate native records after genuine fixtures exist |
| Native `.amc` write | investigating | Explicit, atomic AMC 4.2 export with synthetic byte round trips for metadata, custom values, pictures, and supplementary records; existing destinations receive an fsynced, atomically replaced `.bak` copy before replacement; POSIX directory entries are fsynced after both replacements; injected serialization, backup-copy, and final-replacement interruptions preserve the destination; strict retained-rating, custom-field, separator, and structural metadata validation; configurable budgets exposed by CLI; the string encoder now inverts the reader's lossless preservation of cp1252's five undefined byte positions, a real gap a genuine catalog's round trip found (finding 34), now regression-guarded by a registered synthetic fixture (finding 35); the writer's `year`/`length`/`video_bitrate`/`audio_bitrate`/`media_count` fields now write upstream's own `-1` "no value" sentinel instead of `0` for an unset field (finding 38); a `List`-type custom field now writes its list values at all, instead of the field-type check silently never matching and dropping them, confirmed via a lossless read/write/reread round trip of the genuine, committed `tests/fixtures/native-sample-catalog/` fixtures (finding 39) | Cross-check files in upstream 4.2.3.2 before compatibility claims |
| AMC XML read | partial | Synthetic movie, catalog-property, and custom-field-definition tests; a genuine AMC 4.2.2 export (a large real catalog, contributed by a user for local debugging, not committed to the repository) loaded and round-tripped through `load_xml`/`save_xml` with zero field mismatches after fixing two real bugs it exposed (see design-debt note below) — not registered as a tracked fixture with provenance, so this does not change the status below | Register a provenance-tracked real fixture in the repository and add a documented cross-application test before claiming `verified` |
| AMC XML write | partial | Synthetic movie and metadata self round trip; also exercised against the same real 4.2.2 export locally | Import generated output in upstream AMC |
| CSV read/write | partial | Synthetic round trip; duplicate CSV headers (exact or same-field-after-normalization) are rejected with a clear error instead of silently discarding a column's data | Add upstream locale/delimiter fixtures |
| HTML export | prototype | Atomic escaped table export with bounded document templates and all modeled scalar fields in row templates (AMC Python's own `{{MOVIES}}` syntax); separately, `export-html-template`/`amc.html_template` renders upstream's own `$$TAG_NAME` template syntax (general/item/rating/picture/custom-field tags, the `$$ITEM_BEGIN`/`$$ITEM_END` repeat loop) against real templates a user already has for AMC's HTML export, validated locally against a genuine AMC 4.2.2 export and its own real full/individual templates (a large real catalog, not committed to the repository); the desktop **Export** action offers this template style as an alternative to the default table export when an `.html` destination is chosen | Characterize and implement the `$$ITEM_EXTRA_*` supplementary-record loop, upstream picture/rating-icon file copying, and multi-document export; cross-check tag values against genuine upstream-rendered output |
| Unknown XML fields | partial | Scalar custom values round trip; structured values are rejected rather than flattened | Preserve repetition/order/type/attributes |
| Pictures | investigating | Service/CLI can link, byte/pixel-bound validate/embed, crop, clear, and atomically export movie pictures, including atomic batch clearing and batch set with per-movie crop rectangles (one distinct picture and crop per movie, or one shared picture/crop across several movies) from the CLI (`picture-clear`, `picture-set-many --crop`/`--crop-for`) and the desktop **Set Pictures**/**Assign Pictures**/**Clear Pictures** toolbar actions; the desktop edit dialog and each **Assign Pictures** row offer interactive drag-to-select cropping (`crop_box_from_canvas`/`crop_image_bytes`) instead of numeric coordinates; native read/write retains embedded bytes in JSON; synthetic service/CLI/GUI/native tests | Verify external-path resolution, crop/conversion behavior, and embedded/link semantics with genuine upstream fixtures |
| Catalog metadata | investigating | Native/XML properties retained in validated, deep-copied JSON metadata; explicit error/keep/replace/namespace merge policies | Cross-check policies with genuine fixtures and define typed long-term models |
| Catalog merging | partial | Movie-number `error`/`skip`/`replace`/`renumber` and metadata `error`/`keep`/`replace`/`namespace` policies are available in the model and CLI | Verify policies with genuine interchange fixtures |
| Custom-field definitions | investigating | Native and XML definitions retained in JSON metadata; every native value is additionally retained as an ordered tag/value list so duplicates and reserved-key collisions survive | Cross-check genuine fixtures and typed-value behavior |
| Website scripts | investigating | Bounded, non-executing `.ifs` metadata/permissions/static-name inspection with malformed-entry diagnostics, source-shaped field exclusion delimiters, validated option/parameter configuration, atomic JSON settings persistence, and isolated field-level merge previews that enforce declared movie/picture/extra permissions; static values are not exposed or persisted; 14 genuine, explicitly-licensed community scripts are committed as real fixtures at `tests/fixtures/scripts/` (source and per-file attribution in its `PROVENANCE.md`), validated against a larger 314-file contributor snapshot used only locally, which found and fixed a real crash on scripts in code pages other than cp1252 (`docs/PORT_AUDIT.md` finding 33). Separately, `amc.omdb` (a first-party Python provider, not IFPS) fetches IMDb metadata via the OMDb API and previews/applies it with the same isolated-merge shape, covering the two script use cases named as most-used (update existing entries, IMDb lookups); wired into the CLI as `imdb-lookup` and, since finding 36, the desktop GUI's **Movie / Update from IMDb...** dialog (same preview-then-apply contract, nothing written until the reviewed changes are accepted; the API key is read from `OMDB_API_KEY` by default and never persisted) | IFPS compiler/sandboxed VM and general script execution remain undecided pending an explicit call — real security exposure, not just effort (see `docs/PORT_AUDIT.md` finding 31) |
| Media analysis | prototype | Portable file facts and dependency-free PCM WAV, FLAC, AIFF/AIFF-C, MP3, MP4/M4A/MOV, and OGG Vorbis duration/audio data can create movie entries; MP3 duration/bitrate is computed from the first MPEG audio frame header and file size (exact for CBR files, approximate for VBR files without a parsed Xing/VBRI header); MP4 duration comes from the `moov/mvhd` box, OGG from the Vorbis identification header plus the stream's last granule position, both with only a whole-file average bitrate (no per-codec bitrate field at this level) — all documented limitations, none upstream-verified; `.mp4`/`.m4v`/`.mov` populate `video_format`/`video_bitrate` rather than the audio fields; `movie_from_media` converts the detected duration to whole minutes for `Movie.length`, matching upstream's own documented unit for that field (`options_en.html`) and every other place this port already treats it as minutes — previously an unconverted-seconds bug affected every format since D0 (see `docs/PORT_AUDIT.md` finding 32) | Add video-track resolution/framerate/real codec name (needs per-track sample-table parsing, likely a bounded optional codec provider) and map upstream field/filter semantics |
| Loans | prototype | Source-derived atomic single/multi-movie transitions with opt-in case-insensitive media-label and retained-native-number grouping, validated JSON-retained history, managed/active borrower lists, and atomic source-shaped TSV export; synthetic service/CLI/GUI tests | Verify grouping and TSV encoding/consumer behavior with upstream AMC |
| CLI CRUD | partial | In-process function tests, explicit import policies, shared application services for failure-atomic mutations, exports, and validated backup/restore, read-only protection for native/XML/CSV interchange paths, plus an installed empty-list JSON contract | Expand installed end-to-end command behavior contracts |
| Desktop GUI | prototype | File workflows, responsive multi-row controls, selection/history/read-only/data-aware action states, keyboard search clearing, borrower suggestions, retained loan-history review/export, safe HTTP(S) movie URL launching, atomic multi-row removal and checked-state updates, bounded failure-atomic undo/redo, bidirectional column sorting, validated scalar and multiline description/comment editing, checked/loan filters, table/details/poster/**HTML** layouts — the HTML layout renders the selected movie live through a user-chosen Individual template (`tkinterweb`, this port's second dependency, see ADR-0009 in `docs/decisions.md`), matching upstream's own main window; **Tools → Choose HTML Preview Template...** sets it, persisted in `GuiPreferences` — scaled Pillow decoding plus linked/embedded poster editing, interactive drag-to-select picture cropping, atomic batch picture set/assign/clear across an extended selection (shared or per-movie distinct pictures), persisted view/layout/window-size/undo-redo-depth preferences editable from a toolbar **Preferences** dialog (`amc.preferences`, separate from any catalog file), a cancellable progress-reporting **Import Media** workflow covering both multi-file selection and recursive folder expansion (unlimited or capped at a chosen non-negative subfolder depth), opt-in adjacent CD1/CD2-style multi-part merging, bounded linked/embedded same-stem or folder-poster discovery, full/deferred/skipped metadata-extraction modes, a shared default video-extension set, and bounded filename-title regex cleanup, with an optional comma-separated extension filter on the folder path matching the CLI's `--extensions`, an **Export** action offering Ant Movie Catalog's own `$$TAG_NAME` HTML templates as an alternative to the default table export, presented as one dialog with independently enabled full-catalog-page and individual-movie-page sections (mirroring upstream's own Export screen's separate "Full"/"Individual" templates, minus its in-place template editor — selecting a template file is all this port's renderer needs); every export path then opens an **Export options** dialog offering upstream's own "Movies to include" scope (All/Selected/Checked/Visible, each with a live count) and an export-time sort/reverse control independent of the catalog's own saved order; a second search-bar row — **Search in field** (a specific field or All fields), **Whole field only**, and **Reverse results** — matches upstream's own field-scoped search controls, always live-filtering rather than upstream's separate jump-to-next-match mode, and without its expression-evaluation search mode; **Previous Movie**/**Next Movie** (`Ctrl+PageUp`/`Ctrl+PageDown`) step the table selection by one row with upstream's own no-wraparound behavior at either end; a **File / Edit / Movie / Tools / Help** menu bar covering every action alongside a toolbar slimmed to only the tightest add/edit/remove/toggle/undo/redo loop, a right-click table context menu (Add/Edit/Remove Movie, Toggle Checked, Loan Out, Loan In, Open URL) that selects the clicked row before opening, with menu entries, their matching toolbar buttons, and the context menu kept in lock-step enabled/disabled state, every modal dialog moving initial keyboard focus to a specific control on open, statistics, duplicates, shortcuts, a **Help → About AMC Python...** dialog (version, license, and a project link — upstream has a dedicated Help menu, this port previously had neither), and catalog-path entry point have headless tests; a **Movie / Update from IMDb...** dialog (finding 36) previews an `amc.omdb` OMDb-sourced field-change set for the selected movie and applies it only on confirmation, mirroring the CLI's `imdb-lookup` preview-then-apply contract; every `CatalogService`-call boundary now shares one exception tuple (`CatalogError`, `OSError`, `TypeError`, `ValueError`, `KeyError`) so a stale-movie-number `KeyError` always surfaces as the same error dialog instead of an unhandled callback traceback; native export warns that compatibility is unverified and identifies the replacement backup; a real-display smoke run of the main window plus the crop (interactive drag-select-and-apply), Assign Pictures, Preferences, Import Media, Loan Out (real combobox-and-button interaction verified against the real service), Loan In, Set Pictures, Clear Pictures, and Update from IMDb (a patched OMDb response drives a real fetch-then-apply round trip, plus a failed-lookup path) dialogs, the menu bar's structure, state-sync, and command dispatch, the right-click context menu, statistics/duplicates dialogs, a real check-out event's row in the loan-history dialog, real column-sort click-equivalent reordering and heading-marker toggling, and the edit dialog's out-of-range-rating and non-integer-year validation paths, now runs against a genuine Tk widget tree under Xvfb, automatically wrapped by `tools/check.py` on Linux and wired into the CI Linux job, self-skipping wherever no display exists | Add screen-reader labels and a verified accessibility pass (Tk has no meaningful AT-SPI bridge on X11 to exercise and no screen reader is installed here, so the real-display smoke run above is not a substitute) |
| Web interface (Python extension) | prototype | Read-only responsive table/poster-gallery/details UI with search, bounded pagination, sortable headings, failure-safe external-change reload, desktop-aligned views, security headers, size/pixel-bounded decoded-image MIME handling, HTML escaping, and safe-link tests; independent of Tk | Add authentication/TLS deployment guidance before any mutation support |
| Localization | intentionally omitted | `Common/AntTranslator.pas` (the actual `.lng` loader) shows the mechanism is a runtime Delphi RTTI object-graph patcher tied to live VCL form/component instances (dotted property paths like `Button1.Caption=Fermer`); it has no Tk equivalent, so "parity" with the `.lng` format itself is not a coherent target regardless of effort. A Python-owned i18n layer (externalize `gui.py`'s hardcoded strings behind a key→string lookup, add a loader) is possible but deliberately not built: there is no actual translated content anywhere in this repository to load, so scaffolding now would ship untested, empty infrastructure | Revisit only once a contributor supplies real translated strings to load; until then this is decided, not pending |
| Printing/reports | intentionally omitted | `src/original/FreeReport/license.txt` is LGPLv2, redistributable under this repository's GPLv2 posture — the license question is resolved, not open. What remains is that FreeReport is a complete standalone report designer/renderer (its own binary report format, design-time UI, print preview, large source tree under `src/original/FreeReport/SOURCE/`): an application-sized port, not a bounded slice, disproportionate to the rest of this project's scope | Not planned; `export-html-template`/`amc.html_template` (see HTML export row) already covers "produce a formatted report from the catalog" as a non-compatible baseline. Reconsider only if a specifically PDF/print-friendly export is separately requested |

## Parity accounting notes

The web server, internal JSON format, JSON script-settings files, strict corrupt-file
diagnostics, and safer active-borrower deletion behavior are AMC Python features or
intentional divergences. They are tested functionality, but they do not increase
the count of upstream-verified subsystems. Likewise, accepting an internal JSON
catalog with an `.amc` filename is a migration aid for earlier AMC Python releases,
not support for another native AMC header. The detailed source-by-source gaps are
tracked in the [port audit](PORT_AUDIT.md#gap-matrix-against-the-original-application).

## Supported Python environments

Python 3.10 and newer is declared. A Linux and Windows workflow is configured for
Python 3.10–3.13; this repository does not contain evidence of a completed hosted CI
run. The workflow installs the `dev` extra and runs focused Ruff linting, the full
test suite with an 80% aggregate branch-coverage floor, compilation and source CLI
checks, plus isolated wheel/entry-point smoke tests. Formatting and static type
checking are not yet configured gates. Tk comes from the Python/operating-system
installation rather than a wheel extra; the isolated packaging check explicitly
imports both `tkinter` and `amc.gui` before exercising the GUI entry point.

The detailed evidence and gap analysis is maintained in
[`PORT_AUDIT.md`](PORT_AUDIT.md).

## Updating this file

Every compatibility change must include:

1. Fixture provenance.
2. Automated tests and exact commands.
3. Supported upstream versions.
4. Known lossy fields or behavior.
5. A status change justified in the pull request.
